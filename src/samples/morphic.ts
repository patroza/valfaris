import { AsOpaque, summonFor } from "@morphic-ts/batteries/lib/summoner-ESBAST"
export * from "@morphic-ts/batteries/lib/usage/utils"

export const { summon, tagged } = summonFor({})
export { AsOpaque }
